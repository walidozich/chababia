# PocketBase binary and download archive (not source — re-downloadable)
pocketbase
pocketbase_*.zip

# PocketBase bundled docs (not ours)
CHANGELOG.md
LICENSE.md

# PocketBase runtime data (SQLite databases, generated types, notify state)


# OS / editor noise
.DS_Store
*.swp
